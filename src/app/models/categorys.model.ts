export interface Categorys {
    id: string;          // Identificador único
    nombre: string;      // Nombre de la categoría (e.g., "Tecnología")
    descripcion?: string; // Descripción breve (opcional)
  }